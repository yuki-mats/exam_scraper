# 2002年 問76-100 事前準備メモ

作業対象:

- source: `output/judoseifukushi/questions_json/2002/00_source/question_2002_4.json`
- question labels: `問76`〜`問100`

今回の準備で固定した前提:

- 柔道整復師専用の補助ドキュメントを追加した。
- 03 の説明作成では、`00_source` に説明補助が薄い場合、外部 Web の一次情報を使ってよい。
- 04 の `questionSetId` は、`output/judoseifukushi/category/category.json` の `questionSets[].questionSetId` のみを使う。

この slice で先に確認できたこと:

- `questionType` は全問 `true_false`。
- `questionIntent` の再確認が必要な候補がある。
- 公衆衛生、法規、医療制度、統計の境界が混ざりやすい。

次に見る順番:

1. 01 `questionType` の固定
2. 02 `questionIntent` の修正
3. 03 `explanationText` の一次情報確認
4. 04 `questionSetId` の単元紐付け

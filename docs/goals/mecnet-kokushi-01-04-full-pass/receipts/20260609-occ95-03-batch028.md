# 2026-06-09 occ95 03 batch028

## Scope

- qualification: `mecnet-kokushi`
- occurrence: `95`
- step: `03_prompt_add_explanationText.md`
- reviewed questions in this batch: `8`
- cumulative reviewed in occ95 checkpoint: `224 / 259`

## Local artifact

- local checkpoint: `output/mecnet-kokushi/questions_json/95/21_explanationText_added/wip/manual_review_checkpoint_20260609_batch028.json`
- note: `output/` は `.gitignore` 対象のため、この checkpoint 自体は commit していない

## Reviewed question IDs

217. `83bba67da9d2178a`
   - theme: SLE の症状
   - `lawGroundedExplanationNotNeeded`: `true`
218. `5793c05d527e4cb6`
   - theme: SLE の活動性評価
   - `lawGroundedExplanationNotNeeded`: `true`
219. `339a41e009aa48cb`
   - theme: SLE の治療反応性が乏しい病態
   - `lawGroundedExplanationNotNeeded`: `true`
220. `2c720cd2e1e5b310`
   - theme: 収縮性心膜炎の身体所見
   - `lawGroundedExplanationNotNeeded`: `true`
221. `988f1d3be417d42c`
   - theme: 収縮性心膜炎の心カテ所見
   - `lawGroundedExplanationNotNeeded`: `true`
222. `d37d5e051675da30`
   - theme: 収縮性心膜炎の治療
   - `lawGroundedExplanationNotNeeded`: `true`
223. `a6c815d65498d6f3`
   - theme: 重症急性膵炎の初期対応
   - `lawGroundedExplanationNotNeeded`: `true`
224. `1cf4145f3987844f`
   - theme: 急性膵炎の予後評価画像
   - `lawGroundedExplanationNotNeeded`: `true`

## Verification

```bash
python3 - <<'PY'
import json
from pathlib import Path
source= json.loads(Path('output/mecnet-kokushi/questions_json/95/20_merged_1/question_95_merged.json').read_text())
patch = json.loads(Path('output/mecnet-kokushi/questions_json/95/21_explanationText_added/wip/manual_review_checkpoint_20260609_batch028.json').read_text())
lookup={q['original_question_id']: q for q in source['question_bodies']}
entries=patch['patched_questions']
assert len(entries)==8
for e in entries:
    q=lookup[e['original_question_id']]
    assert isinstance(e['lawGroundedExplanationNotNeeded'], bool)
    assert len(e['explanationText'])==len(q['choiceTextList'])
    assert len(e['suggestedQuestions'])==len(e['suggestedQuestionDetails'])
    for detail, question in zip(e['suggestedQuestionDetails'], e['suggestedQuestions']):
        assert detail['question']==question
print('checkpoint ok:', len(entries))
PY
```

```bash
python3 - <<'PY'
import json
from pathlib import Path
paths=[*['output/mecnet-kokushi/questions_json/95/21_explanationText_added/wip/manual_review_checkpoint_20260609_batch%03d.json'%i for i in range(1,29)]]
ids=[]
for p in paths:
    data=json.loads(Path(p).read_text())
    ids.extend(e['original_question_id'] for e in data['patched_questions'])
assert len(ids)==len(set(ids))==224
print('cumulative unique reviewed:', len(ids))
PY
```

Results:

- `checkpoint ok: 8`
- `cumulative unique reviewed: 224`

## Remaining baseline

- occ95 `03` checkpoint remaining: `35`
- full kokushi `03` checkpoint remaining: `12,836`
- full `01→04` final completion remaining: `13,060`

## Next

- occ95 の `03` を question 225 以降へ継続
- cumulative reviewed を増やしつつ、最終的に occ95 の 259 問全件を正式 patch 化する
- 全体最終目標は引き続き `13,060問` の `01→04` 完了である

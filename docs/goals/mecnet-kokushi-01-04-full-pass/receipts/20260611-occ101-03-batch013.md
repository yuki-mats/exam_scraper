# 2026-06-11 occ101 03 batch013

## Scope

- qualification: `mecnet-kokushi`
- occurrence: `101`
- step: `03_prompt_add_explanationText.md`
- reviewed questions in this batch: `8`
- source order: `97〜104`
- cumulative reviewed in occ101 checkpoint: `104 / 500`

## Local artifact

- local checkpoint: `output/mecnet-kokushi/questions_json/101/21_explanationText_added/wip/manual_review_checkpoint_20260611_batch013.json`

## Reviewed question IDs

1. `b2a8089d532255c1`
   - theme: 好中球は細菌貪食、好塩基球は炎症物質放出を担う
   - `lawGroundedExplanationNotNeeded`: `true`
2. `6ea6cf87adc1d2d1`
   - theme: ADHは尿を濃縮し、エリスロポエチンは腎臓から分泌される
   - `lawGroundedExplanationNotNeeded`: `true`
3. `fd22394ecf9b1d4b`
   - theme: 無月経と直接関係がないのは卵管である
   - `lawGroundedExplanationNotNeeded`: `true`
4. `38cea6b6b84ee24f`
   - theme: させられ体験は思考障害ではなく意思・自我意識の障害である
   - `lawGroundedExplanationNotNeeded`: `true`
5. `8f464a67c1987564`
   - theme: 手の温痛覚線維は脊髄で交叉する
   - `lawGroundedExplanationNotNeeded`: `true`
6. `3785251444d14c5d`
   - theme: Willis動脈輪の後交通動脈は④である
   - `lawGroundedExplanationNotNeeded`: `true`
7. `7815faf63558e5e7`
   - theme: 成人の正常関節軟骨にはコラーゲンとヒアルロン酸が存在する
   - `lawGroundedExplanationNotNeeded`: `true`
8. `db4e3ff4b4d0ba70`
   - theme: オキシトシンは射乳を促進し、成長ホルモンはインスリン低血糖で増加する
   - `lawGroundedExplanationNotNeeded`: `true`

## Verification

```bash
python3 - <<'PY'
import json
from pathlib import Path
source=json.loads(Path('output/mecnet-kokushi/questions_json/101/20_merged_1/question_101_merged.json').read_text())
patch=json.loads(Path('output/mecnet-kokushi/questions_json/101/21_explanationText_added/wip/manual_review_checkpoint_20260611_batch013.json').read_text())
lookup={q['original_question_id']: q for q in source['question_bodies']}
entries=patch['patched_questions']
assert len(entries)==8
false_ids=[]
for e in entries:
    q=lookup[e['original_question_id']]
    assert isinstance(e['lawGroundedExplanationNotNeeded'], bool)
    assert len(e['explanationText'])==len(q['choiceTextList'])
    assert len(e['suggestedQuestions'])==len(e['suggestedQuestionDetails'])
    assert 3 <= len(e['suggestedQuestions']) <= 5
    for detail, question in zip(e['suggestedQuestionDetails'], e['suggestedQuestions']):
        assert detail['question']==question
        assert detail.get('answer')
    if not e['lawGroundedExplanationNotNeeded']:
        false_ids.append(e['original_question_id'])
print('checkpoint ok:', len(entries))
print('false ids:', false_ids)
PY
```

```bash
python3 - <<'PY'
import json
from pathlib import Path
paths=sorted(Path('output/mecnet-kokushi/questions_json/101/21_explanationText_added/wip').glob('manual_review_checkpoint_20260611_batch*.json'))
ids=[]
for p in paths:
    data=json.loads(p.read_text())
    ids.extend(e['original_question_id'] for e in data['patched_questions'])
assert len(ids)==len(set(ids))==104
print('cumulative unique reviewed:', len(ids))
print('paths:', ', '.join(p.name for p in paths))
PY
```

Results:

- `checkpoint ok: 8`
- `false ids: []`
- `cumulative unique reviewed: 104`
- `paths: manual_review_checkpoint_20260611_batch001.json, manual_review_checkpoint_20260611_batch002.json, manual_review_checkpoint_20260611_batch003.json, manual_review_checkpoint_20260611_batch004.json, manual_review_checkpoint_20260611_batch005.json, manual_review_checkpoint_20260611_batch006.json, manual_review_checkpoint_20260611_batch007.json, manual_review_checkpoint_20260611_batch008.json, manual_review_checkpoint_20260611_batch009.json, manual_review_checkpoint_20260611_batch010.json, manual_review_checkpoint_20260611_batch011.json, manual_review_checkpoint_20260611_batch012.json, manual_review_checkpoint_20260611_batch013.json`

## Remaining baseline

- occ101 `03` checkpoint remaining: `396`
- full `01→04` final completion remaining: `20` occurrences (`101〜120`)

## Notes

- 今回の8問は白血球機能、腎生理、無月経の原因部位、精神症候、神経伝導路、脳血管解剖、関節軟骨、下垂体ホルモンであり、法令・制度依存の正誤判断は見当たらなかったため全件 `true` とした。
- Firestore live upload と実機テストは実施していない。

## Next

- occ101 の `03` を source order 105 以降へ継続
